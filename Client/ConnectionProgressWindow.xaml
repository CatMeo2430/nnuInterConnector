<Window x:Class="Client.ConnectionProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="互联协商" 
        Height="320" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        FontFamily="Segoe UI, Microsoft YaHei">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F8F9FA" Offset="0"/>
                <GradientStop Color="#E9ECEF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="White" CornerRadius="16" Margin="12" BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
                    <Border Background="#E3F2FD" CornerRadius="8" Padding="10" Margin="0,0,12,0">
                        <TextBlock Text="对方ID" FontSize="13" FontWeight="Medium" Foreground="#1976D2"/>
                    </Border>
                    <TextBox x:Name="TargetIdTextBox"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Width="140" Height="36" 
                             FontSize="13"
                             VerticalContentAlignment="Center"
                             ToolTip="输入对方的6位数字ID"/>
                    <Button x:Name="StartButton"
                            Style="{StaticResource ModernButtonStyle}"
                            Content="开始" 
                            Height="36"
                            Padding="16,0"
                            Margin="12,0,0,0"
                            Click="StartButton_Click"
                            ToolTip="开始发起互联请求"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="正在建立连接..." FontSize="16" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ProgressBar x:Name="ProgressBar" 
                                 Grid.Row="0" 
                                 Height="8" 
                                 Margin="0,10,0,16"
                                 Foreground="#0078D4"
                                 Background="#E0E0E0"
                                 BorderThickness="0"/>

                    <TextBlock x:Name="StatusText" 
                               Grid.Row="1" 
                               Text="准备就绪" 
                               FontSize="14" 
                               Foreground="#666666"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"/>
                </Grid>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="CancelButton"
                            Style="{StaticResource ModernButtonStyle}"
                            Content="取消" 
                            Background="#6C757D"
                            Height="36"
                            Padding="16,0"
                            IsEnabled="False"
                            Click="CancelButton_Click"
                            ToolTip="取消本次互联请求"/>
                    <Button x:Name="CloseButton"
                            Style="{StaticResource ModernButtonStyle}"
                            Content="关闭" 
                            Height="36"
                            Padding="16,0"
                            Margin="8,0,0,0"
                            Click="CloseButton_Click"
                            ToolTip="关闭窗口"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
